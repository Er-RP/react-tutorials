import "./ElementSelector.css";

const ElementSelector = () => {
  return (
    <>
      <p className="title title--left">Element Selector</p>
      <li>ElementSelector is used to select specific element</li>
      <p>Css properties are shared between same elements</p>
      <li>For example both `li` elements have same css properties</li>
    </>
  );
};

export default ElementSelector;
